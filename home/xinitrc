# .xinitrc
# Source library file
library_file=$HOME/.dotfiles/lib.sh
[ -f $library_file ] || { printf "No library file: %s\n" "$library_file"; exit 1; }
. $library_file

# Set Wallpaper
feh --no-fehbg --bg-fill --recursive --randomize $wallpaper_dir &

# Set Xmodmap if file exists
[ -f $HOME/.Xmodmap ] && xmodmap $HOME/.Xmodmap
# Run xrdb to load .Xresources if file exists
[ -f $HOME/.Xresources ] && xrdb -merge $HOME/.Xresources

# Start Window Manager
exec $window_manager
